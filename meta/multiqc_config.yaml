report_comment: >
  This report has been generated by the <a href="https://github.com/PavlidisLab/sc-annotation-pipeline" target="_blank">PavlidisLab/sc-annotation-pipeline</a>
  analysis pipeline.

data_format: 'yaml'

run_modules:
    - custom_content

custom_data:
    celltype_composition:
        section_name: 'Sample cell type composition'
        description: 'test'
        file_format: 'tsv'
        plot_type: 'table'
        pconfig:
            id: 'celltype_composition_plot'
            title: 'Sample cell type composition'
            namespace: 'Sample cell type composition'
            
    outlier_composition:
        section_name: 'Sample outlier composition'
        description: 'test'
        file_format: 'tsv'
        plot_type: 'table'
        pconfig:
            id: 'outlier_composition_plot'
            title: 'Sample outlier composition'
            namespace: 'Sample outlier composition'

    markers_heatmap:
        section_name: 'Cell type markers'
        description: 'test'
        file_format: 'tsv'
        plot_type: 'heatmap'
        pconfig:
            id: 'markers_heatmap_plot'
            title: 'Cell type markers'

    markers_table:
        section_name: 'Cell type markers table'
        description: 'test'
        file_format: 'tsv'
        plot_type: 'table'
        pconfig:
            id: 'markers_table_plot'
            title: 'Cell type markers table'

sp:
  markers_heatmap:
    fn: "*heatmap_mqc.tsv"

  outlier_composition:
    fn: "*_outlier_counts_mqc.tsv"

  celltype_composition:
    fn: "*_celltype_counts_mqc.tsv"
  
  markers_table:
    fn: "*_celltype_markers_mqc.tsv"


custom_data:
  order:
    - celltype_composition
    - outlier_composition
    - cell_type_markers
    - markers_table



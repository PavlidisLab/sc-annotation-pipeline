{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$id": "https://raw.githubusercontent.com/PavlidisLab/sc-annotation-pipeline/master/assets/schema_input.json",
    "title": "nf-core/scannotate pipeline - params.input schema",
    "description": "Schema for the input samplesheet used by the pipeline",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "sample": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Sample name must be provided and cannot contain spaces",
                "meta": ["id"]
            },
            "study_name": {
                "type": "string",
                "pattern": "^\\S*$",
                "errorMessage": "Study name cannot contain spaces (leave empty if providing study_path)"
            },
            "study_path": {
                "type": "string",
                "format": "directory-path",
                "exists": true,
                "errorMessage": "Study path must be a valid directory path (leave empty if providing study_name)"
            }
        },
        "required": ["sample"]
    }
}

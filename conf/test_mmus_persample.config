params {
    config_profile_name        = 'Mouse per-sample test profile'
    config_profile_description = 'Mouse test dataset with process_samples=true to check the per-sample processing path'

    input = 'assets/samplesheet.csv'

    organism        = 'mus_musculus'
    census_version  = '2024-07-01'
    subsample_ref   = 500
    process_samples = true
    cutoff          = 0
    seed            = 42
    nmads           = [mito: 20, umi: 5, genes: 5, counts: 5]
    use_staging     = true

    ref_collections = [
        "A taxonomy of transcriptomic cell types across the isocortex and hippocampal formation",
        "An integrated transcriptomic and epigenomic atlas of mouse primary motor cortex cell types",
        "Tabula Muris Senis"
    ]
    original_celltype_columns  = "${projectDir}/assets/original_celltype_columns.tsv"
    author_annotations_path    = "${projectDir}/assets/author_cell_annotations"

    preferredCtaLevel = 'family'
    upload_cta = true
    upload_clc = true
    upload_mask = true
    upload_multiqc = true
}
